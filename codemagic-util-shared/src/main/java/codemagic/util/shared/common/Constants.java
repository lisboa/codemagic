package codemagic.util.shared.common;

import java.util.Locale;

public interface Constants {

	String EMPTY_STRING = "";
	
	/** The GWT only supports the ROOT, US and ENGLISH locales */
	Locale DEFAULT_LOCALE = Locale.US;
}
